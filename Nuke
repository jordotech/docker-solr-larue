eval $(docker-machine env solr)
docker-compose stop;
docker-machine stop solr;
docker-machine rm solr -y;
docker-machine create -d virtualbox solr;
eval $(docker-machine env solr)
docker rm -f $(docker ps -a -q);
#docker rmi $(docker images -q)
docker-compose build;
docker-compose up -d;
